# Separate cargo project for cargo-fuzz.
#
# `cargo-fuzz` requires a nightly toolchain and is run manually; CI does not
# exercise these targets.  See `README.md` in this directory for the workflow.
#
# This crate is intentionally *not* a workspace member: cargo-fuzz manages its
# own build profile and would conflict with the workspace's resolver settings.
[package]
name = "lantern-core-fuzz"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata]
cargo-fuzz = true

# An empty workspace table keeps this crate out of the repository workspace.
# (The root manifest's `exclude` entry alone is not enough: the crate sits
# inside the `crates/lantern-core` member directory.)
[workspace]

[dependencies]
lantern-core = { path = ".." }
libfuzzer-sys = "0.4"
url = "2"

[[bin]]
name = "fuzz_parser"
path = "fuzz_targets/fuzz_parser.rs"
test = false
doc = false
bench = false

[[bin]]
name = "fuzz_url_treatments"
path = "fuzz_targets/fuzz_url_treatments.rs"
test = false
doc = false
bench = false

[profile.release]
debug = 1
